class GameSettings {
    players = [];
    width;
    height;

    constructor(width, height, players) {
        this.width = width;
        this.height = height;
        this.players = players;
    }
}
